<h1><%= @book.title %></h1>

<p><%= @book.body %></p>

<ul>
  <li><%= link_to "Start page", root_path %></li>
  <li><%= link_to "Edit", edit_book_path(@book) %></li>
  <li><%= link_to "Delete book", book_path(@book), data: {
    turbo_method: :delete,
    turbo_confirm: "Are you sure?"
  } %></li>
</ul>